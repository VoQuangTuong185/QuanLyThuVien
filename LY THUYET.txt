1.[Warning] deprecated conversion from string constant to 'char*' [-Wwrite-strings]

=> vì C và C++ khác nhau ở string literal.
Trong C thì string là mảng của char
Trong C-++ thì string là mảng không đổi của Char

 trying to convert a string literal (with type const char[]) to char*

void foo(char* str);
foo((char *)"hello");
That is, simply cast the string constant to (char *).

- Có 2 cách đọc file khi sử dụng header file fstrea, : toán tử >> và hàm getline
- Có 4 loại: đọc từng kí tự (variable là char), đọc từng dòng (getline), đọc từng từ (varible is string), đọc file CSV

=> test push to Tuong